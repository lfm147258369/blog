---
date: 2025-04-06
tags:
  - 类与对象
---

# 类模板案例

实现一个通用数组类

- 可放自定义数据类型
- 可以存储到堆区(动态)
- 构造函数中传入数组容量
- 提供对应的拷贝构造函数以及operator=防止浅拷贝问题
- 提供尾插法和尾删法对数组中的数据进行增加和删除
- 可以通过下标的方式访问数组中的u元素
- 可以获取数组中当前元素个数和数组的容量

```cpp
class MyArray
{
	//数组
	
	//容量
	
	//大小
}
```